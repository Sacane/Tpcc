parametres : 
Au départ on a les paramtres dans les registres.

On reserve la place des parametres comme ci c'était des variables locales

fun:
	;add(ou sub) rsp, <Offset total de la fonction>
	;rbp + <offset de la variable local> -> rbp sauvegarde la position du pointeur de la pile au moment d'appeler la fonction, on réserve donc le nombre d'offset voulu

    